/**
 * An interface for XML that extends the Endpoint interface.
 */
package RESTController.Interfaces;

/**
 * @author Mike Dietz
 *
 */
public interface XML extends Endpoint {
	//Enforce additional constraints here otherwise leave blank
}
